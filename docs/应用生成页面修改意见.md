修改应用生成页面布局：
    1-应用编辑页面不展示global-header和global-footer页面主体覆盖整页面
    2-将app-info-card和preview-toolbar合并，作为页面header，高度60px
    3-页面header的新布局如下（未提及组件删除，不确定请向我询问）：中原先app-info-card的内容靠最左侧；部署按钮靠右侧，部署按钮左侧加一个<VerticalAlignBottomOutlined />按钮占位；中间加一个 <a-radio-group>，有两个选项<DesktopOutlined />和<CodeOutlined />，前者用iframe渲染页面，后者留占位符即可，<a-radio-group>的左侧应当与preview-body的左侧对齐。
    2-chat-board和preview-body比例改为1:3
    

修改msg-row的渲染方式：
    1-用户消息不展示头像，消息气泡保持右对齐并占据父容器宽度80%
    2-ai消息头像大小改为圆形直径16px，消息气泡最大宽度直接占满剩余宽度
    3-每个消息气泡下方增加一个时间显示，显示消息的createTime。两者垂直排布，用户消息的createTime右对齐，ai消息左对齐。格式串为：x月x日 xx:xx | AIGC (AIGC仅展示于ai消息)

    